# Scalping System Requirements
# Add these to your main requirements.txt if not already present

# Core dependencies (should already be installed)
pandas>=1.3.0
numpy>=1.21.0
scikit-learn>=0.24.0
joblib>=1.0.0
MetaTrader5>=5.0.0

# Machine Learning & Ensemble Models
xgboost>=1.5.0
lightgbm>=3.2.0
catboost>=0.26.0

# Advanced Time Series (Optional but recommended for LSTM features)
tensorflow>=2.6.0  # Optional: For advanced deep learning features
torch>=1.9.0      # Optional: For PyTorch-based models

# Data Processing
scipy>=1.7.0
matplotlib>=3.4.0
seaborn>=0.11.0

# Async & Background Tasks (for future enhancements)
asyncio-contextmanager>=1.0.0
python-dotenv>=0.19.0

# Logging & Monitoring
python-json-logger>=2.0.0

# API & Web (for future dashboard)
fastapi>=0.68.0      # Optional: For API server
uvicorn>=0.15.0      # Optional: For API server
websockets>=10.0     # Optional: For real-time updates
redis>=4.0.0         # Optional: For signal queue

# Database (optional, for trade logging)
sqlalchemy>=1.4.0
psycopg2-binary>=2.9.0

# Development & Testing
pytest>=6.2.0
black>=21.0          # Code formatter
flake8>=3.9.0        # Linter
mypy>=0.910          # Type checking
